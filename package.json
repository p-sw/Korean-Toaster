{
  "name": "karetlang",
  "packageManager": "yarn@4.6.0",
  "scripts": {
    "setup": "pip install -r requirements.txt",
    "build": "pyinstaller --windowed -n KoreanToaster -i icon.ico -y --add-data icon.png:icon.png --clean main.py",
    "dev": "py main.py"
  }
}
