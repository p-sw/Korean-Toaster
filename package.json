{
  "name": "karetlang",
  "packageManager": "yarn@4.6.0",
  "scripts": {
    "setup": "pip install -r requirements.txt",
    "build": "pyinstaller --windowed -n KoreanToaster --noconfirm main.py",
    "dev": "py main.py"
  }
}
